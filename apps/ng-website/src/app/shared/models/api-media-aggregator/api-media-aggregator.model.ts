export interface ApiListFilterForm {
  category: string;
  company: string;
}

export interface ApiChannelFilterPipeArgs {
  company: string;
  currentCategory: string;
  category: string;
}
