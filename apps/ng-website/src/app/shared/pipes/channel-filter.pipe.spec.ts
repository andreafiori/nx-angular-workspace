import { ChannelFilterPipe } from './channel-filter.pipe';

describe('ChannelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
